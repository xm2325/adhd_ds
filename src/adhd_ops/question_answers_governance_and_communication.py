from __future__ import annotations
from typing import Any


def build_answers(ctx: dict[str, Any]) -> dict[str, str]:
    return {'access_control': 'Aggregate endpoints are open in the demo, while appointment-support returns 403 to the default executive role and only a field-minimised response to operational roles. The header is a demonstration, not production authentication.', 'reproducibility': f"Run {ctx['summary']['run_id']} records configuration hashes, seven source fingerprints and output hashes. The manifest can be replayed to verify whether a later output is identical.", 'incident_response': f"There are {ctx['incidents_open']} open or triage incidents linked to the run. Each has severity, owner, playbook and rollback trigger; red controls should never remain only as dashboard colour.", 'decision_request': f"The immediate synthetic decision is whether to validate and resource extra assessment capacity, because backlog ends at {ctx['_fmt'](ctx['baseline_scenario']['end_backlog'], 0)} and the dominant stage is {ctx['top_stage']['stage_label'].lower()}. There are {ctx['action_open']} open actions requiring named owners.", 'executive_message': f"Headline: assessment capacity is the dominant synthetic service constraint. Evidence: P90 wait {ctx['_fmt'](ctx['summary']['p90_referral_to_assessment_days'])} days and baseline backlog {ctx['_fmt'](ctx['summary']['baseline_backlog_end'], 0)}. Decision: confirm the wait definition and feasibility of additional assessment minutes; do not treat reminder assumptions as evidence.", 'first_30_days': 'Days 1–10: map decisions, stakeholders, systems and metric owners. Days 11–20: reconcile sources, define patient-pathway metrics and install publication gates. Days 21–30: deliver one trusted operational view and agree the first measurable experiment or capacity decision.', 'non_conclusions': 'Do not interpret synthetic values as provider performance, decomposition as causality, model importance as intervention effect, queue policy as clinical priority, cost proxy as actual finance data, or a browser/header role demo as production security.'}
